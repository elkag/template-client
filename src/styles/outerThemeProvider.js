import { createMuiTheme } from "@material-ui/core";

export const outerTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#ef5350',
        },
        secondary: {
          main: '#00897b',
        },
    },
  });